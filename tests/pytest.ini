[pytest]
# run from the repository root:  python -m pytest -c tests/pytest.ini
# rootdir stays here (tests/) on purpose — see the note in pyproject.toml.
testpaths = .
addopts = --import-mode=importlib -q
norecursedirs = .git __pycache__
